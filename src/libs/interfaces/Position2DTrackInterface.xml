<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE interface SYSTEM "interface.dtd">
<interface name="Position2DTrackInterface" author="Masrur Doostdar" year="2009">
  <data>
    <comment>
      This interface provides access to a track of 2D positions.
    </comment>

    <field type="float" name="track_x_positions" length="30">
      X-Positions of the track. The first array-element is the oldest position of the track, 
      the last is the newest.
    </field>
    <field type="float" name="track_y_positions" length="30">
      Y-Positions of the track. The first array-element is the oldest position of the track, 
      the last is the newest.
    </field>
    <field type="int32" name="track_timestamps" length="30">
      Timestamps of the track. The first array-element is the oldest position of the track, 
      the last is the newest.
    </field>
    <field type="bool" name="valid">True, if this track is valid.</field>
    <field type="uint32" name="length">Length of the Tracks (i.e. up to which index there are valid positions).</field>
    <field type="uint32" name="track_id">The ID of the Track.</field>
  </data>
</interface>
